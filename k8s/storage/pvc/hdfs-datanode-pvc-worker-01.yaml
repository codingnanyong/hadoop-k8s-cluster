apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hdfs-datanode-pvc-worker-01
  namespace: hadoop
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-hdfs
  resources:
    requests:
      storage: 500Gi

