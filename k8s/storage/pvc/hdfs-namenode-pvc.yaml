apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hdfs-namenode-pvc
  namespace: hadoop
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-hdfs
  resources:
    requests:
      storage: 20Gi
